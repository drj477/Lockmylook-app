name: Apply profile ring geometry fix

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fix profile rings
        shell: python
        run: |
          from pathlib import Path

          path = Path('mobile/lib/features/home/presentation/screens/home_screen.dart')
          text = path.read_text(encoding='utf-8')

          start = text.index('  Widget _profileBubble({')
          end = text.index('\n  Widget _quickCard(', start)

          new_function = r'''  Widget _profileBubble({
    required String label,
    required String initial,
    required bool selected,
    String? avatarUrl,
    IconData? icon,
    VoidCallback? onTap,
  }) {
    final isAdd = icon != null;

    return GestureDetector(
      onTap: onTap,
      child: SizedBox(
        width: 66,
        child: Column(
          children: [
            SizedBox(
              width: 64,
              height: 64,
              child: Stack(
                alignment: Alignment.center,
                children: [
                  // Every profile gets the same outer coral ring.
                  // The selected profile only changes this ring's thickness.
                  if (!isAdd)
                    Container(
                      width: 64,
                      height: 64,
                      decoration: BoxDecoration(
                        shape: BoxShape.circle,
                        border: Border.all(
                          color: LockMyLookUi.coral,
                          width: selected ? 3.5 : 1.5,
                        ),
                      ),
                    ),
                  // Every profile also gets the same inner coral ring.
                  if (!isAdd)
                    Container(
                      width: 56,
                      height: 56,
                      decoration: BoxDecoration(
                        shape: BoxShape.circle,
                        border: Border.all(
                          color: LockMyLookUi.coral,
                          width: 1.5,
                        ),
                      ),
                    ),
                  Container(
                    width: 48,
                    height: 48,
                    decoration: BoxDecoration(
                      shape: BoxShape.circle,
                      color: isAdd ? Colors.white : LockMyLookUi.coralSoft,
                      border: isAdd
                          ? Border.all(color: LockMyLookUi.border, width: 1.5)
                          : null,
                    ),
                    child: ClipOval(
                      child: isAdd
                          ? Icon(icon, color: LockMyLookUi.navy, size: 23)
                          : avatarUrl != null && avatarUrl.isNotEmpty
                              ? Image.network(
                                  avatarUrl,
                                  fit: BoxFit.cover,
                                  errorBuilder: (_, _, _) => Center(
                                    child: Text(
                                      initial,
                                      style: const TextStyle(
                                        color: LockMyLookUi.coral,
                                        fontWeight: FontWeight.w800,
                                        fontSize: 18,
                                      ),
                                    ),
                                  ),
                                )
                              : Center(
                                  child: Text(
                                    initial,
                                    style: const TextStyle(
                                      color: LockMyLookUi.coral,
                                      fontWeight: FontWeight.w800,
                                      fontSize: 18,
                                    ),
                                  ),
                                ),
                    ),
                  ),
                ],
              ),
            ),
            const SizedBox(height: 6),
            Text(
              label,
              maxLines: 1,
              overflow: TextOverflow.ellipsis,
              style: const TextStyle(fontSize: 11, color: LockMyLookUi.ink),
            ),
          ],
        ),
      ),
    );
  }
'''

          text = text[:start] + new_function + text[end:]
          path.write_text(text, encoding='utf-8')

      - name: Commit and remove workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mobile/lib/features/home/presentation/screens/home_screen.dart
          git commit -m "fix: make profile rings consistent"
          git rm .github/workflows/fix-profile-rings.yml
          git commit -m "chore: remove temporary ring fix workflow"
          git push
